<% column :full, :id => "nav-main" do %>
  <ul>
    <%= tab "Forums", forums_path %>
    <% if current_user %>
      <%= tab current_user.full_name, profile_path %>
      <%= tab "Logout", logout_path %></li>
    <% else %>
      <%= tab "Log in here", login_path %>
    <% end %>
  </ul>
<% end %>